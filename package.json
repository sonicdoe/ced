{
  "name": "ced",
  "version": "1.0.0",
  "description": "Detect the character encoding using Google’s compact_enc_det library",
  "keywords": [
    "character",
    "charset",
    "detect",
    "detection",
    "encoding",
    "google"
  ],
  "license": "(ISC AND Apache-2.0)",
  "repository": "sonicdoe/ced",
  "author": "Jakob Krigovsky <jakob@krigovsky.com>",
  "files": [
    "binding.gyp",
    "ced.cpp",
    "index.js",
    "vendor/compact_enc_det"
  ],
  "scripts": {
    "test": "clang-format --Werror --dry-run --style=Google *.cpp && standard && ava"
  },
  "dependencies": {
    "bindings": "^1.3.0"
  },
  "devDependencies": {
    "ava": "^3.12.1",
    "clang-format": "^1.5.0",
    "standard": "^16.0.3"
  },
  "engines": {
    "node": ">=10"
  }
}
